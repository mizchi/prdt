name = "mizchi/prdt"

version = "0.1.0"

readme = "README.md"

repository = "https://github.com/mizchi/prdt"

license = "Apache-2.0"

keywords = [
  "crdt",
  "prdt",
  "consensus",
  "replicated-data-type",
  "local-first",
]

description = "Replicated domain objects: pure domain state machines composed with a PRDT-style replicated finalization protocol"

source = "src"

import {
  "mizchi/experimental_crypto@0.0.2",
}
