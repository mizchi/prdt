name = "mizchi/prdt_mmo"

version = "0.1.0"

license = "Apache-2.0"

description = "MMO sample for mizchi/prdt: lethal-race domain, simulations, and the JSON bridge behind the Cloudflare room host"

source = "src"

import {
  "mizchi/prdt@0.1.0",
}
